import { Card } from "@mui/material";

const ItemCard = (props) => {
    return (  
        <Card variant="outlined" sx={{maxWidth: "150px", maxHeight: "200px"}}>
            sadas
        </Card>
    );
}
 
export default ItemCard;